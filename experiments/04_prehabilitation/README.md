# 04 — Functional prehabilitation

Temporarily adapts only the target block with LoRA while preserving original logits, then discards that block and deploys the same fixed-compute surrogate class. Teacher drift was negligible; deployed test KL did not improve.

