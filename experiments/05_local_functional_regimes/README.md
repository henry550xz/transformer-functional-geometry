# 05 — Local functional regimes

Estimates pointwise H=32 downstream Fishers and compares local Grassmann subspaces. Pointwise ranks were concentrated, but train-only prototype dictionaries barely outperformed one matched global subspace on test.

