# 01 — KV predictability

Tests independent coding, reconstructed previous-token delta, reference residuals, and train-only AR baselines under rate–distortion evaluation. The public snapshot contains aggregate results only; raw KV tensors and encoded streams are excluded.

